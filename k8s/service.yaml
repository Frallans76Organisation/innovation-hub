apiVersion: v1
kind: Service
metadata:
  name: innovation-hub
  namespace: innovation-hub
  labels:
    app: innovation-hub
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: innovation-hub
