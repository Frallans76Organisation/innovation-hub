apiVersion: v1
kind: Service
metadata:
  name: {{ include "innovation-hub.fullname" . }}
  labels:
    {{- include "innovation-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "innovation-hub.selectorLabels" . | nindent 4 }}
